# Contributing

- Target **Unreal Engine 5.8**.
- Do not commit `Binaries/`, `Intermediate/`, `Saved/`, `DerivedDataCache/`, or generated `.sln` / `.slnx` files.
- Keep pull requests to source, config, and content that should ship.
- Design notes are not in this repo.
