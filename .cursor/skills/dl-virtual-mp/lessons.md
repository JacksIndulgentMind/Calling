# Two-box lessons

Append a line when a two-window join, travel, or two-agent drive fails in a new way. Method, not a one-hop coordinate.

- Loopback UDP is the virtual socket. Named pipes, TAP adapters, and shared-file “net drivers” are parked. World sim stays `IpNetDriver`.
- Host first. Guest without `-CallingAgentHttpPort=18767` fails 18765/18766 bind (expected). With the flags, guest MCP is 18767/18768. Director `virtualhost` / `ready` / `go` only on the host.
- Drive the guest pawn from guest HTTP 18767 or host hub `connectMode=proxy`. Do not `mindControl` the guest human from the host lobby — that puppets the server copy and skips the `ServerMove` the two windows exist to test. Do not skip guest `ServerMove` or `PerformMovement` the remote pawn on the listen server.
- Do not re-`appendBotBook` to “kick” a live `goto`. Append used to LIFO-`PushBook` + `CancelGoto`, so the leaf never walked and 100 copies piled up. Append now queues; live book is `botBook.name`. Replan is `branchBotBook` with `cause` (`execution` = bot failed the book — **fails the drive** via `executionError` / hub `alert: botbook_execution`; `situation` = combat/personality; storm if 8 situation branches in 4s). A poller that posted `cause=execution` then kept waiting on `modeResult` hid those errors. `botbook_stack` if frames exceed 32.
- Guest connect is `ClientTravel` to `127.0.0.1:7777` (command line map URL, or **Virtual join**). Composer Host/Guest on one window is still same-process.
- Beacon is `FPaths::ProjectDir()/Saved/Calling/loopback-host.json`, not UserDir. Give the guest `-UserDir=` so logs and profiles do not fight; both still read the same beacon.
- `ACLGreyboxFloors` replicates. Any `DOREPLIFETIME` property needs `Replicated` or `ReplicatedUsing` or the **guest fatals** on join. `Layout` uses `OnRep_Layout` so the client stamps pads locally (runtime cubes are not the net channel). Guest Recast must bake too (`RebuildNavigation` on the client after OnRep); catalog `goto` books skip when `navTiles` is 0 on that process.
- Lobby seats live on host GameInstance. Net guests need `EnsureNetHuman` + `BoundController`, GameState `LobbySeats` snaps for the guest composer menu, and Ready/team Server RPCs. Do not map every `PlayerController` to the first human.
- `RestoreBodiesAfterTravel` must not wipe seats that have `BoundController`. After PvP `ServerTravel` (`?listen` kept), `/state.lobby.seats` can dip to 1 until the guest re-joins the new map — wait, then confirm two humans.
- Guest auto-ready on composer `HandleStartingNewPlayer`. Host still Ready+Go. Min players 2. Do not auto-Go.
- Default `/state` is the host pawn. Two agents: host `/state?seat=` per host cursor; guest MCP samples 18767 for local prediction. Replication proof is always host 18765. Never one probe for two bodies.
- Ring verify (`dl-circle-run`) is **one** process + hub guest. Two-box is a different bring-up; do not replace ring with listen+ClientTravel unless the user asked for two windows.
- Listen-server CMC only walks a pawn that is `IsLocallyControlled()` (or the server copy of an AutonomousProxy that is receiving `ServerMove`). Recast `goto` on a body that is not local still logs stick and repaths; loc stays spawn and `ctrlYaw` stays spawn facing. Halt and check possess/local on **both** processes — do not retune JumpLength. Guest books must tick on the guest client; host Red must stay possessed by the listen-server local PC after PvP `?listen` travel (guest rejoin at 20.38.34 in the shrine-clash two-box log). Composer `GameAndUI` is not restored to `GameOnly` when the menu is torn down on scene change — restore that when touching input.
- Hub drive trace: `LogCallingHub` `HubDrive` / `PC HubRecv`. Stamp `listenPort`+`recv` and **`instanceId`/`agentId`/`requestorId`** on every hub command; caller sends `agentId` and `intendedTarget`. `ok: false` `remote_player_pawn` if the driven pawn is a net-human whose `PlayerController` is not local (wrong window). `ignoreMove`/`ignoreLook`/`unlocked` are on the same line. `goto tick` repeats `inst=`/`agent=`/`requestor=` for the seat that requested the walk. Guest proxy ingress stamps `recv=http` like 18767.
- **Wrong probe ≠ glued Blue.** After a pass two-box walk, a script still reported `blueMoved=False` / `x=6380` for 30s. That was the poll, not the pawn. Path B `join` mints the cursor on the **guest**; that `seatId` is absent from host 18765 `/state`. Host `?seat=` only knows **this process’s** seats. Replication proof of Blue is host `?seat=<this session’s Guest BoundController id from lobby.seatList>` — never a GUID copied from an earlier run (the fail used `9A443962-…` while lobby Guest was `10802ACE-…`). Guest prediction is `GET :18767/state?seat=<the proxy join cursor>`. Hub `appendBotBook` `ok` with `drivenLocal=true` `execPort=18767` `recv=http` already means the guest ingress ran the book. If those replies succeed and both windows walked, do not treat the stale/wrong `?seat=` as a stuck pawn, and do not retune JumpLength from that fail gate.
- **`while: fire` must not replace `goto` stick.** `ApplyAgentIntent` assigns `AgentMove` every call. `FGotoVerb::Tick` walks, then `CLApplyBotWhile(..., false)` used to PulseIntent with Move=0 + Fire=true, so both pawns stood at spawn and shot. Fail the walk as soon as loc stays ±6380 while the book is firing — do not wait for `modeResult` / failTimeout. Latch fire/ADS without writing Move. A JIT `goto` with no `while:` still walks (that is not a Recast miss).
- **Poller success is “command followed,” not `modeResult`.** `gotoDistXY` is the **current waypoint**, not the marker. Loc frozen on a live `goto` with goal still far is `loc_still` even if the waypoint DistXY is small (kissing a menhir at 682,-120 while `court_center` is 8 m away). Do not re-append the same marker; peek `hide_center_lee` then resume. Spawn idle with no `goto` is also a fail. Empty book + `enemyDistXY` ≲ 1 m + `agentFire=false` is a statue, not a gunfight — splice `track_fire` (verb must keep `agentFire=true`; `lastFollowAlert` can be stale). A PowerShell throw while `modeResult` stays `in_progress` is not a match fail — the engine must `FinishMode` (`modeFailReason` = the followAlert code) and `/state.events` must show it. Never resume that instance. Script: `Scripts/dl-assert-command-followed.ps1`.
- **loc_still dump was missing z/fwd/ge.** Last fail `goalDist=451 wpStuck=1.4s` loc=(227,10) did not say air, fwd, or goodEnough. Event `detail` now includes z, air, dive, flight, fwd, reason, move, blocked, ge/inGe, idx/wp, repathLeft. Do not change loc_still 0.5s/80cm from a thin dump.
- **loc_still is 3D.** Red `goto hide_center_lee` at court `(220,-29)` was rocket-jumping (`z` −167 → +176, `fwd=drop/50`) but still used DistXY only, so 0.5 s of vertical travel was `pawn still`. Reset the still anchor when 3D Dist > 80 cm. A true perch (XY and Z frozen) still fails. Do not skip loc_still for every `!IsMovingOnGround()` (that hid the lee perch). Do not weaken 0.5s/80cm.
- **Half-cover vault vs `JumpableHeadClearCm`.** Recast `goto shrine_tree` from court put a waypoint on the lee hide top `(64,416,-500)`. Waist probe was `cover/50` (vaultable) but `bJumpCover` required head Dist ≥ 500 cm, which is never true inside `JumpFaceCm` (head Dist ≈ waist Dist). Enabling vault on every Cover then made Blue jump the spawn `cover_full` (220 cm) and loc_still at ~(6051,−254). Vault only when Cover rise is within NavTune `CoverHeightCm` (140): lee 85 cm yes, pad-lip 220 cm no (wall-slide around). Do not weaken loc_still.
- **Recast AirDive waypoint already inside `GotoWaypointRadius`.** After the Launch look fix, Red landed shrine_well then `goto court_center` armed `ARM_LAUNCH` DistXY=87 (`envelope=NO tooShort`) because Recast flagged that poly AirDive. TickWalk will not skip-advance an AirDive index, so Launch "flew" in place, `bFlight` hid loc_still, then land in the same 80 cm circle → `pawn still 1.5s` at (−1993,1882) goalDist=2846. If DistXY to the hop end is already ≤ waypoint radius (120), clear the AirDive flag and walk-advance. Reset `PawnStillSeconds` on each new leaf so a shrine arrive does not inherit still into the next `goto`. Do not weaken loc_still 0.5s/80cm.
- **Recast Launch vs `while: trackFocus`.** A live `goto` that owns an AirDive poly arms Launch (`bFlight`) and waits to face the hop (18°) before jump pulses. `FGotoVerb::Tick` then ran `while: trackFocus`, which `SetLookTrackSeat`s the enemy and clears that look goal. The pawn sat grounded at the lee/menhir ring (~−474,754) for 22s, `wpStuck=0`, `ARM_LAUNCH` TO≈(−1500,1939) DistXY≈1585, and loc_still skipped the whole time because skip is `bFlight`. Do not wait forever for facing (slew budget ~0.45s). Skip `trackFocus` look while `bFlight`; keep fire latch. Do not weaken loc_still 0.5s/80cm.
- **loc_still far is the leaf settle, not a hidden 500cm band.** `bFar` is DistXY outside GoodEnough (else Success, else 300). Inflating catalog `goodEnough` so a stall counts as settled is suppression. `hopOff` (jump-to-unstick) is gone. `shrine_clash_fight` starts at `hide_center_lee` with the same ~180/280 band as court — no `menhir_2_approach` detour. Assert helper trusts engine `followAlert` / `botbook_*`; it does not duplicate loc_still DistXY. Do not weaken 0.5s/80cm.
- **loc_still is pose and speed.** Red `goto` at `(117,358)` `z=-317` `air=1` `fwd=drop/50` `velXY=231` still fired 0.5s (goalDist=486, ge=280). Jumping or walking off a lip is not a perch. Reset the still clock when 3D Dist > 80 **or** velocity Size > 80. A true perch (pose and speed frozen, goal outside settle) still fails. Do not skip loc_still for every `!IsMovingOnGround()`. Do not weaken 0.5s/80cm.
- **loc_still with stick on, fwd=open, vel crawl.** After the velocity gate, Red grounded `velXY=43` `fwd=open/1800` `reason=wp` while loc oscillated ~20 cm (capsule scraping a vertical face). Floor-column + line-trace only labeled Walk on walkable slopes, so wall-slide never saw Wall. Sweep an AgentRadius sphere along WalkDir; non-walkable blocking hits are Wall. Do not catalog a menhir detour. Do not weaken 0.5s/80cm.
- **wall-slide into the face.** Sphere sweep named `fwd=wall/40` `reason=wallSlide` but loc still crawled (`velXY=32`) on an 80 cm post: Left/Right probes used the WalkDir-backed waist, missed the same face, and ToSteer picked the into-obstacle tangent. Slide along the hit tangent and peel along the hit normal. Lateral fallback probes from pawn loc. Do not weaken 0.5s/80cm.
- **Empty book is a statue, not a match clock.** After tangent+peel, Red walked and scored 3–0 + `shrine_held`. Then both books finished (`goto=false` `book=` `fire=false`) at the SE shrine ~`(1628,-1919)` / `(1562,-2017)`. loc_still only runs on a live `goto`, so no `botbook_*`. Polling `modeResult` for 180s while loc was frozen hid that. `onStop` restarts the catalog (not a self-`ref`). Combat `onRespawn` without `onStop` that exhausts in-progress is `botbook_idle`. Do not wait on `zero_kills` / shrine rotation.
- **Lee-first spine farms one side to 10.** `onStop` looped; Red hit 10 with Blue still on the east pad (`zero_kills`). The objective is `/state.liveShrine`, not hide_center_lee. Start the clash book at `live_shrine` so both walks meet the occupy tag. Do not weaken `failIfEitherTeamKillsZero`.
- **Far floor Cover hid a near Wall.** After live_shrine-first, Red loc_still at court `fwd=cover/680` `velXY=30` `wp=(-64,153)` (center post). Floor-column returned Cover 6.8 m out and skipped the AgentRadius sweep. If the sweep Wall is ≥40 cm nearer than the floor hit, use the sweep so wall-slide can run. Do not weaken 0.5s/80cm.
- **Guest hitscan never killed.** Two-box `zero_kills` 10–0 with Blue `agentFire=true` and both `shrine_held`: host listen applies hitscan locally; guest `FireShot` damaged only the client replica of Red, then replication restored HP. `ApplyDamage` is authority-only. Owning-client fire is predict-FX + `ServerHitscanFire` damage + `MulticastHitscanFX`; grenades spawn on authority (`ServerGrenadeFire`). Never client `ApplyDamage`. Do not treat a 10–0 with the guest spraying as a BotBook personality miss. Do not weaken `failIfEitherTeamKillsZero`.
- **Fire `success: enemyDistXY > 2200` + `onStop` is a spin.** After guest hitscan, Blue led 7–2 with shrine credit then `timeout`. Occupy fire succeeded in 0.5s whenever the enemy was walking from spawn (far/dead), `goto live_shrine` was already Success, `onStop` re-appended at NetHz (dozens of `botbook_append` at one loc). Occupy fire settles on `fail.timeout` only (`z > 99999`), ~8s. Do not succeed a hold because the enemy is far — that is when you occupy. Do not weaken `failTimeoutSeconds`.
- **Far-only shrine is a turret.** `if enemyDistXY < 2200` at book start is almost always false from spawn, so both only occupied. Guest hitscan then 0–4 / 2–7: one side farms the live shrine, the other dies on the approach (`zero_kills` at timeout). Same book both seats: shrine occupy then lee peek every loop so they leave the turret. Do not weaken `failIfEitherTeamKillsZero`.
- **Winner.** Two-box `modeResult=winner` kills 1/10, both `shrine_held`, `mode_winner` in events. Guest hitscan + shrine-then-lee spine. Dump events and stop; do not resume a fail instance.
